{
  "name": "eventhub",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "concurrently --names \"API,WEB\" --prefix-colors \"cyan,magenta\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "setup": "npm install --prefix backend && npm install --prefix frontend",
    "seed": "npm run seed --prefix backend",
    "migrate": "cd backend && npx prisma migrate dev",
    "db:push": "cd backend && npx prisma db push",
    "build": "npm run build --prefix frontend",
    "lint": "npm run lint --prefix frontend",
    "format": "prettier --check \"pages/**/*.ts\" \"fixtures/**/*.ts\" \"tests/**/*.ts\" playwright.config.ts",
    "format:fix": "prettier --write \"pages/**/*.ts\" \"fixtures/**/*.ts\" \"tests/**/*.ts\" playwright.config.ts",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:report": "playwright show-report",
    "test:docker": "docker build -t eventhub-e2e . && docker run --rm eventhub-e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^25.9.2",
    "concurrently": "^9.1.2",
    "prettier": "^3.5.3",
    "typescript": "^5.9.3"
  }
}
